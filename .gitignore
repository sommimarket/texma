.claude/
logo-original.png
1.png
2.jpg
3.jpg

# cache de wrangler (no va al repo)
server/.wrangler/

# ---- Capacitor / APK ----
node_modules/
# www/ se genera con `npm run build:web` a partir de los archivos de la raíz
www/
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
android/capacitor-cordova-android-plugins/
*.apk
*.aab

# ⚠ el keystore de firma NUNCA va al repo: sin él no podés actualizar la app
*.keystore
*.jks
android/keystore.properties

# credenciales de deploy (service_role) — JAMAS al repo
.env

# firma del APK de produccion — el keystore y sus claves JAMAS al repo
android/keystore.properties
*.keystore
*.jks
